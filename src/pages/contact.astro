---
import Layout from '../layouts/BaseLayout.astro';
const title = 'Kontak | AetherSub';
---

<Layout title="Kontak & Support">
  <section class="mx-auto max-w-3xl px-6 pt-32 pb-16">
    <h1 class="mb-6 text-center text-3xl font-extrabold text-lime-600">Kontak & Support</h1>

    <p class="mb-10 text-center leading-relaxed text-gray-600">
      Ada pertanyaan, kritik, atau butuh bantuan? Silakan hubungi kami melalui formulir berikut,
      atau kontak langsung via email & WhatsApp.
    </p>

    <!-- Info Box -->
    <div class="mb-12 rounded-2xl border border-lime-200 bg-lime-50 p-6 shadow-sm">
      <h2 class="mb-3 text-lg font-bold text-lime-700">Informasi Kontak</h2>

      <div class="space-y-2 text-gray-700">
        <p>
          <i class="fa-solid fa-envelope mr-2 text-lime-600"></i>
          Email: <a
            href="mailto:helpdesk@aethersub.store"
            class="font-medium text-lime-700 hover:underline">helpdesk@aethersub.store</a
          >
        </p>

        <p>
          <i class="fa-brands fa-whatsapp mr-2 text-lime-600"></i>
          WhatsApp: <a
            href="https://wa.me/6285171140818"
            class="font-medium text-lime-700 hover:underline">+62 851-7114-0818</a
          >
        </p>

        <p>
          <i class="fa-solid fa-clock mr-2 text-lime-600"></i>
          Jam Layanan: 08.00 – 17.00 WIB
        </p>
      </div>
    </div>

    <!-- Contact Form -->
    <div class="rounded-2xl border border-gray-100 bg-white p-8 shadow-xl">
      <h2 class="mb-6 text-xl font-bold text-gray-800">Kirim Pesan</h2>

      <form class="space-y-5">
        <div>
          <label class="mb-1 block text-sm font-medium text-gray-700">Nama Lengkap</label>
          <input
            id="name"
            type="text"
            class="w-full rounded-xl border border-gray-300 px-4 py-3 focus:ring-2 focus:ring-lime-500 focus:outline-none"
            placeholder="Masukkan nama kamu"
            required
          />
        </div>

        <div>
          <label class="mb-1 block text-sm font-medium text-gray-700">Email</label>
          <input
            id="email"
            type="email"
            class="w-full rounded-xl border border-gray-300 px-4 py-3 focus:ring-2 focus:ring-lime-500 focus:outline-none"
            placeholder="nama@email.com"
            required
          />
        </div>

        <div>
          <label class="mb-1 block text-sm font-medium text-gray-700">Pesan</label>
          <textarea
            id="message"
            rows="5"
            class="w-full rounded-xl border border-gray-300 px-4 py-3 focus:ring-2 focus:ring-lime-500 focus:outline-none"
            placeholder="Tulis pesan kamu di sini..."
            required></textarea>
        </div>

        <button
          type="button"
          onclick="sendWhatsAppMessage()"
          class="w-full transform rounded-xl bg-lime-600 py-3 font-semibold text-white transition hover:scale-[1.02] hover:bg-lime-700"
        >
          Kirim Pesan
        </button>
      </form>
    </div>

    <!-- Script -->
    <script is:inline>
      function sendWhatsAppMessage() {
        const name = document.getElementById('name')?.value.trim();
        const email = document.getElementById('email')?.value.trim();
        const message = document.getElementById('message')?.value.trim();

        if (!name || !email || !message) {
          alert('Harap isi semua kolom sebelum mengirim pesan.');
          return;
        }

        const phone = '6285171140818';

        const text = `Halo AetherSub, saya ingin mengirim pesan:

Nama: ${name}
Email: ${email}

Pesan:
${message}
`;

        const waUrl = `https://wa.me/${phone}?text=${encodeURIComponent(text)}`;
        window.open(waUrl, '_blank');
      }
    </script>
  </section>
</Layout>
